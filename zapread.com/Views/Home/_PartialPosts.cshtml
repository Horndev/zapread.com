@model zapread.com.Models.PostsViewModel

@foreach (var p in Model.Posts)
{
    // We mark the first post with identifiers used in the tour
    @Html.Partial(partialViewName: "_PartialPostRenderVm", model: p)
}
